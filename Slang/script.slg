
void Next(string in, string sin)
{
	string green = "bean"
	green += " "
	green += in

	print in
	print sin
	print green
}

void Main(string input, int in2)
{
	int x = 1

	int a = 0
	int b = 1

	print input
	print in2
	
	int k = 0

	while x < 100
	{
		int k = 1 / (1+2.71828^-x)
		print k
		print "X is " + x
		string str = x

		// str=  0+(" :: ")
		str += " :: "
		str += k
		
		x += 10
		print str
	}
	Next "seen", "bop"
}
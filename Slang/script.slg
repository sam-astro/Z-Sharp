
void Next(string in, string sin)
{
	string green = "bean"
	green += " "
	green += in

	print in
	print sin
	print green
	return "awe"
}

void Main(string input, int in)
{
	print pi
	int x = 1

	int a = 0
	int b = 1

	print input
	print in
	
	int k = 0

	while x < 100
	{
		int k = 1 / (1+2.71828^-x)
		print k
		float s = Sin(x)
		print "Sin is " + s
		
		x += 10
	}
	print Next("seen", "bop")
}